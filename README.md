# Todo API

Simple REST API for managing tasks (Todos) built with Node.js, TypeScript, Express, and MongoDB.

---

## Installation & Running

1. Clone the repository:

```bash
git clone <repository>
cd <project-folder>
```
